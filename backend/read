//for changing pwd
 ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY '23520008';

mysql> FLUSH PRIVILEGES;


//backend

npm init -y
npm install express mysql2 cors bcryptjs jsonwebtoken body-parser

//frontend

ng new school-management --routing
cd school-management
npm install jwt-decode

ng generate component pages/login
ng generate component pages/register
ng generate component dashboards/student
ng generate component dashboards/teacher
ng generate component dashboards/admin
ng generate service auth




//database

CREATE DATABASE IF NOT EXISTS school_db;
USE school_db;

CREATE TABLE IF NOT EXISTS users (
  id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(100),
  email VARCHAR(100) UNIQUE,
  password VARCHAR(255),
  role ENUM('student', 'teacher', 'admin') NOT NULL
);
select * from users;

CREATE TABLE result (
  id INT AUTO_INCREMENT PRIMARY KEY,
  student_id VARCHAR(255) NOT NULL,
  email VARCHAR(255) NOT NULL,
  subject1 INT,
  subject2 INT,
  subject3 INT,
  subject4 INT,
  subject5 INT
);
select * from result;